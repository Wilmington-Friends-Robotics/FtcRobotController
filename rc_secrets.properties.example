# Copy this file to rc_secrets.properties (which is git-ignored)
# and set per-robot network identity values.
#
# RC_WIFI_NAME must satisfy FTC naming rules and end in "-RC"
# (example: 7584-RC).
#
# RC_WIFI_PASSWORD must be 8-63 characters.

RC_WIFI_NAME=7584-RC
RC_WIFI_PASSWORD=wfs1748!
